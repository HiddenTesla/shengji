# 🃏 升级（拖拉机 / 80分）纯网页端实现计划

## 一、游戏概述

**升级**（俗称拖拉机、80 分、双升）是一种流行于中国的四人扑克牌游戏。本项目使用 **TypeScript + Vite + React** 构建**纯网页端**单机游戏。

### 核心规则（基于两副牌/四人玩法）

| 项目 | 说明 |
|------|------|
| 玩家 | 4 人，以 **东南西北** 四方表示，**南家为玩家本人**，对家组队（南北 vs 东西） |
| 牌具 | 2 副标准牌（含大小王），共 **108 张** |
| 发牌 | 每人 **25 张**，留 **8 张底牌** |
| 级牌 | 当前打几，该点数的牌即为级牌（如打 5，所有 5 是级牌） |
| 主牌 | 叫主确定的牌的花色 + 级牌 + 大小王 |
| 分牌 | 5=5 分，10=10 分，K=10 分 |
| 胜利 | 从 2 打到 A 即获胜（一局）；也可计分多局 |

### 玩家操控模式

| 特性 | 说明 |
|------|------|
| 方位 | 东(E) · 南(S) · 西(W) · 北(N)，**南家固定为玩家本人** |
| 手动/托管 | 每家均可独立切换「手动」或「托管」模式 |
| 默认 | **南北为手动**，东西为托管（AI 操控） |
| 托管时 | 该方位由 AI 自动完成叫主、扣底、出牌等所有操作 |
| 手动时 | 等待玩家操作（若为南家则直接操作；若为北/东/西家手动，则玩家替其操作） |
| 牌面显示 | 除南家外，东、西、北家的牌可独立切换「显示」或「隐藏」 |
| 默认隐藏 | 东、西、北家默认牌背朝上（隐藏），可点击切换为牌面朝上（显示） |

---

## 二、项目架构

```
shengji/
├── src/
│   ├── core/                       # 核心游戏逻辑（纯 TS，无 UI 依赖）
│   │   ├── types.ts                # 全局类型定义
│   │   ├── card.ts                 # 卡牌定义与比较
│   │   ├── deck.ts                 # 牌组、洗牌、发牌
│   │   ├── rank.ts                 # 等级 & 升级规则
│   │   ├── trump.ts                # 主牌判定
│   │   ├── bidding.ts              # 叫主逻辑（亮主/反主/无主 + AI）
│   │   ├── hand.ts                 # 手牌管理 & 合法出牌检测（最复杂）
│   │   ├── trick.ts                # 出牌 & 墩比较
│   │   ├── scoring.ts              # 计分逻辑
│   │   ├── game.ts                 # 游戏状态机（主循环）
│   │   └── ai.ts                   # AI 策略（规则驱动，无学习能力）
│   │
│   ├── components/                 # React UI 组件
│   │   ├── GameBoard.tsx           # 主游戏面板（东南西北四方布局）
│   │   ├── CardComponent.tsx       # 单张卡牌（SVG 渲染，支持牌面/牌背）
│   │   ├── PlayerHand.tsx          # 南家手牌（可点击选中出牌）
│   │   ├── OpponentHand.tsx        # 东/西/北家手牌（支持显示/隐藏切换）
│   │   ├── TrickArea.tsx           # 出牌区（显示当前墩）
│   │   ├── ScoreBoard.tsx          # 记分板
│   │   ├── BiddingBar.tsx          # 四花色报主栏（南家，随发牌启用）
│   │   ├── BottomCards.tsx         # 扣底界面
│   │   ├── GameLog.tsx             # 牌局日志
│   │   ├── GameControls.tsx        # 游戏控制按钮（新局、重发等）
│   │   └── PlayerModePanel.tsx     # ♟ 模式面板：四家手动/托管切换 + 牌面显示/隐藏开关
│   │
│   ├── hooks/                      # 自定义 Hooks
│   │   └── useGame.ts              # 游戏状态管理 Hook
│   │
│   ├── utils/                      # 工具函数
│   │   ├── replay.ts               # 牌局导入/导出（JSON 序列化）
│   │   └── sound.ts                # 音效管理
│   │
│   ├── App.tsx                     # 根组件
│   ├── main.tsx                    # 入口
│   └── index.css                   # 全局样式
│
├── public/                         # 静态资源
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── PLAN.md                         # 本文件
└── README.md
```

### 架构特点

```
┌─────────────────────────────────────────┐
│              React UI 层                 │
│  GameBoard → PlayerHand / TrickArea /…  │
└────────────────┬────────────────────────┘
                 │ 调用
┌────────────────▼────────────────────────┐
│           核心游戏引擎 (core/)            │
│  纯 TypeScript，零外部依赖                │
│  Game → Hand → Trick → Scoring → AI     │
└─────────────────────────────────────────┘
                 │
                 ▼
        ┌─────────────────┐
        │  Replay (JSON)   │  ← 导入/导出
        └─────────────────┘
```

- **无服务器**：所有逻辑在浏览器端运行
- **无联机**：4 人在同一浏览器，南家为玩家，其余默认 AI 托管
- **核心引擎与 UI 完全解耦**：`core/` 目录为纯 TypeScript，可独立测试
- **每家可独立切换模式**：手动（等待玩家操作）或 托管（AI 自动操作）
- **东/西/北家牌面可切换**：默认隐藏（牌背），可点击显示（牌面）

---

## 三、技术栈

| 层级 | 选型 |
|------|------|
| 语言 | **TypeScript** |
| 构建 | **Vite 6** |
| UI 框架 | **React 19** |
| 样式 | **TailwindCSS v4** |
| 测试 | **Vitest**（核心逻辑） |
| 包管理 | **npm** |
| Lint | **ESLint + Prettier** |

---

## 四、Todo List

| # | 模块 | 任务描述 | 预计工时 | 依赖 | 状态 |
|---|------|----------|----------|------|------|
| # | 模块 | 任务描述 | 预计工时 | 依赖 | 状态 |
|---|------|----------|----------|------|------|
| 1 | **core/types** | 定义 Suit、Rank、Card、Player(东南西北)、Team(南北/东西)、GameState 等全局类型 | 1h | - | ✅ |
| 2 | **core/card** | Card 类：花色、点数、级牌/主牌判定、比较运算符、排序 | 1.5h | #1 | ✅ |
| 3 | **core/deck** | Deck：创建两副 108 张、Fisher-Yates 洗牌、发牌（每人 25+8底） | 1h | #1, #2 | ✅ |
| 4 | **core/rank** | RankManager：等级管理（2→A 共 13 级）、升级判定逻辑 | 0.5h | #1 | ✅ |
| 5 | **core/trump** | 主牌判定：叫主结果 → 确定主花色、级牌、大小王排名链 | 1.5h | #1, #2, #4 | ✅ |
| 5b | **core/calling** | 叫主逻辑：亮主/反主/无主、强度判定、AI 叫主策略 | 2h | #1, #2, #4, #5 | ✅ |
| 6 | **core/hand** | ⭐ **牌型检测**：单张/对子/拖拉机/连对/甩牌检测算法 | 4h | #1, #2, #5 | ⬜ |
| 7 | **core/hand** | ⭐ **合法出牌生成**：跟牌规则（同花色必须跟/垫牌/毙牌） | 4h | #1, #2, #5, #6 | ⬜ |
| 8 | **core/trick** | 墩比较：同花色比大小、主牌 > 副牌、牌型压制规则 | 2h | #1, #2, #5, #6 | ⬜ |
| 9 | **core/scoring** | 计分：分牌累计、80分上台、升级跳级、**抠底计分**（单×2/对×4/拖×8） | 2h | #1, #4, #8 | ⬜ |
| 10 | **core/game** | 游戏状态机：WAITING→DEALING→CALLING→BURYING→PLAYING→ROUND_END→GAME_END | 3h | #1-#9 | ⬜ |
| 11 | **core/ai** | AI 策略：简单（随机合法牌）/中等（优先出分/毙牌）/困难（记牌、配合） | 3h | #1-#10 | ⬜ |
| 12 | **core/ai** | AI 叫主策略：根据手牌强度决定是否叫主、选花色 | 1h | #5, #11 | ✅ |
| 13 | **core/ai** | AI 扣底策略：分牌沉底/保底牌等 | 1h | #9, #11 | ✅ |
| 14 | **UI 工程** | Vite + React + TailwindCSS 项目初始化，东南西北基础布局 | 1.5h | - | ⬜ |
| 15 | **UI CardComponent** | 单张卡牌 SVG 渲染组件（花色符号、点数、选中高亮） | 2h | #14 | ⬜ |
| 16 | **UI PlayerHand** | 南家手牌：点击选中/取消、出牌确认 | 2h | #14, #15 | ⬜ |
| 17 | **UI OpponentHand** | 东/西/北家手牌：牌背朝上（默认隐藏），支持切换为牌面显示 | 1.5h | #14, #15 | ⬜ |
| 18 | **UI TrickArea** | 四家出牌区：动画显示当前墩的出牌 | 1.5h | #14, #15 | ⬜ |
| 19 | **UI ScoreBoard** | 计分板：当前等级、南北/东西得分、升级进度条 | 1h | #14 | ⬜ |
| 20 | **叫主倒计时窗口** | 发牌后进入倒计时窗口（默认 8s），窗口内南家随时可报主/反主（无弹窗）；到点定主，无人则翻底 | 2h | #5b | ✅ |
| 20b | **UI BiddingBar** | 四花色报主栏（南家）：彩色花色 + 非级牌数量 + 报主/反主/无主，随逐张发牌启用 | 2h | #14, #5b | ✅ |
| 20c | **发牌动画** | 逐张发牌（Dealing 阶段），南家手牌逐一揭示 | 1.5h | #3 | ✅ |
| 20d | **翻底定主** | 无人报主时逐张翻底牌（动画），翻到级牌→其花色 / 王→无主即停；都没有则翻完取最大点数花色；翻完停 3 秒定主。「翻底定主」可点击展开翻牌记录。此后不可反主 | 1.5h | #5b | ✅ |
| 21 | **UI BottomCards** | 扣底界面：从庄家手牌选 8 张埋底（含 AI 托管自动扣底） | 1.5h | #14, #15, #16 | ✅ |
| 22 | **UI GameBoard** | 主游戏面板：整合所有子组件，东南西北四方方位布局 | 2h | #14-#21 | ⬜ |
| 23 | **UI GameLog** | 游戏日志：出牌记录、得分变化、升级提示 | 1h | #14 | ⬜ |
| 24 | **UI GameControls** | 控制按钮：新游戏、重新发牌、AI 速度控制 | 0.5h | #14 | ⬜ |
| 25 | **UI PlayerModePanel** | ♟ 模式面板：四家独立手动/托管切换 + 东/西/北家牌面显示/隐藏开关 | 1.5h | #14 | ⬜ |
| 26 | **集成** | useGame Hook：core/ 引擎 ↔ UI 状态绑定（含手动/托管模式判断） | 3h | #10, #22 | ⬜ |
| 27 | **集成** | 托管时 AI 自动出牌 + 延迟动画（模拟思考） | 1.5h | #11, #26 | ⬜ |
| 28 | **集成** | 叫主 → 扣底 → 出牌完整流程联调（含手动/托管切换任意组合） | 2h | #26, #27 | ⬜ |
| 29 | **♻️ 牌局导入/导出** | JSON 序列化整个 GameState + replay 播放器 | 3h | #10, #26 | ⬜ |
| 30 | **🎨 美化** | 卡牌背面图案、牌桌背景、出牌/得分/升级动效 | 3h | #22 | ⬜ |
| 31 | **🔊 音效** | 出牌、得分、升级、叫主音效（Web Audio API） | 1.5h | #22 | ⬜ |
| 32 | **🧪 测试** | 核心逻辑单元测试（发牌、牌型检测、墩比较、计分） | 3h | #1-#10 | ⬜ |
| 33 | **📱 响应式** | 移动端/小屏幕适配 | 2h | #22 | ⬜ |
| 34 | **🐛 全面测试** | 边界情况测试（抠底、拖拉机压制、甩牌、反主、手动/托管切换） | 2h | #32 | ⬜ |

> 状态图例：⬜ 未开始 · 🔄 进行中 · ✅ 已完成

---

## 五、阶段里程碑

### Milestone 1 — 核心引擎（#1 ~ #13）
- 纯 TypeScript 游戏逻辑完整可运行
- 通过单元测试验证 4 个 AI 自动完成一局游戏

### Milestone 2 — 可玩 UI（#14 ~ #28）
- 完整东南西北四方牌桌界面
- 玩家（南家）可操作叫主、扣底、出牌
- 东/西家默认托管（AI），南北家默认手动
- 可随时切换任意一家的手动/托管模式
- 东/西/北家牌面可独立切换显示/隐藏

### Milestone 3 — 高级功能（#29 ~ #34）
- 牌局导入/导出
- 音效、动画、美化
- 响应式 + 全面测试

---

## 六、核心算法难点

### 6.1 拖拉机检测算法

```
输入：手牌中某花色的所有牌（已排序）
输出：所有可能的拖拉机组合

思路：
1. 按点数分组，过滤出成对的牌
2. 将成对的牌按点数排序
3. 扫描连续点数 >= 2 的对子序列
4. 级牌处理：级牌可以当作主花色的对子，也可当作副花色的对子
5. 特殊情况：级牌相邻的牌需要考虑（如打 5，4 和 6 的拖拉机仍然成立）
```

### 6.2 合法出牌生成算法

```
输入：当前手牌、已出的首出牌（如果是跟牌）
输出：所有合法的出牌组合

跟牌逻辑：
1. 如果有首出花色的牌，必须出该花色
2. 如果没有该花色，可以垫牌（出任意副牌）或用主牌"毙"
3. "毙"时如果对方也有拖拉机，必须出拖拉机
```

### 6.3 抠底计分

```
底牌分值 × 倍数
- 最后一墩出单张 → ×2
- 最后一墩出对子 → ×4
- 最后一墩出拖拉机（2对） → ×8
- 每多一对拖拉机 → ×2 递增
```

### 6.4 甩牌合法性判定

```
玩家一次出多张牌时，必须验证所有对手都管不上其中任何一张牌
实现：回溯搜索，检查每个对手是否有压制组合
```

---

## 七、文件清单

| 文件 | 预估行数 | 难度 | 优先级 |
|------|----------|------|--------|
| `src/core/types.ts` | 100 | ⭐ | P0 |
| `src/core/card.ts` | 150 | ⭐⭐ | P0 |
| `src/core/deck.ts` | 80 | ⭐ | P0 |
| `src/core/rank.ts` | 60 | ⭐ | P0 |
| `src/core/trump.ts` | 120 | ⭐⭐ | P0 |
| `src/core/hand.ts` | 400 | ⭐⭐⭐⭐⭐ | P0 |
| `src/core/trick.ts` | 150 | ⭐⭐⭐ | P0 |
| `src/core/scoring.ts` | 100 | ⭐⭐ | P0 |
| `src/core/game.ts` | 300 | ⭐⭐⭐⭐ | P0 |
| `src/core/ai.ts` | 400 | ⭐⭐⭐⭐ | P0 |
| React 组件（合计） | ~2000 | ⭐⭐⭐ | P1 |
| `src/utils/replay.ts` | 150 | ⭐⭐ | P1 |
| 单元测试 | ~1000 | ⭐⭐ | P1 |
| 样式/动画/音效 | ~500 | ⭐⭐ | P2 |

**总计预估**：约 **3500-4000 行代码**，**2-3 周**（单人兼职）

---
```

### 5.3 抠底计分

```
底牌分值 × 倍数
- 最后一墩出单张 → ×2
- 最后一墩出对子 → ×4
- 最后一墩出拖拉机（2对） → ×8
- 每多一对拖拉机 → ×2 递增
```

---

## 六、文件清单与预估工作量

| 文件 | 预估行数 | 难度 | 优先级 |
|------|----------|------|--------|
| `packages/core/src/types.ts` | 100 | ⭐ | P0 |
| `packages/core/src/card.ts` | 150 | ⭐⭐ | P0 |
| `packages/core/src/deck.ts` | 80 | ⭐ | P0 |
| `packages/core/src/rank.ts` | 60 | ⭐ | P0 |
| `packages/core/src/trump.ts` | 120 | ⭐⭐ | P0 |
| `packages/core/src/hand.ts` | 400 | ⭐⭐⭐⭐⭐ | P0 |
| `packages/core/src/trick.ts` | 150 | ⭐⭐⭐ | P0 |
| `packages/core/src/scoring.ts` | 100 | ⭐⭐ | P0 |
| `packages/core/src/game.ts` | 300 | ⭐⭐⭐⭐ | P0 |
| `packages/core/src/player.ts` | 30 | ⭐ | P0 |
| `packages/server/src/index.ts` | 100 | ⭐⭐ | P1 |
| `packages/server/src/room.ts` | 200 | ⭐⭐⭐ | P1 |
| `packages/server/src/session.ts` | 200 | ⭐⭐⭐ | P1 |
| `packages/server/src/ai/aiPlayer.ts` | 300 | ⭐⭐⭐⭐ | P1 |
| `packages/server/src/protocol.ts` | 80 | ⭐⭐ | P1 |
| Web 前端（整体） | ~2000 | ⭐⭐⭐ | P2 |
| 单元测试 | ~1500 | ⭐⭐ | P0-P2 |

**总计预估**：约 **4000-5000 行代码**，**8-12 个工作日**（单人）

---

## 七、开发顺序建议

```
Week 1:
  周一: Phase 1 — 类型定义 + 发牌 + 测试
  周二: Phase 2 — 主牌判定 + 出牌规则(hand.ts难点)
  周三: Phase 2 — 墩比较 + 计分 + 游戏状态机
  周四: Phase 3 — WebSocket 服务器 + 房间管理
  周五: Phase 3 — AI 玩家 + 游戏会话

Week 2:
  周一: Phase 4 — 前端项目搭建 + 核心组件
  周二: Phase 4 — 游戏界面交互
  周三: Phase 4 — 联调测试 + Bug 修复
  周四: Phase 5 — 高级功能
  周五: 全面测试 + 部署
```

---

## 八、风险与应对

| 风险 | 应对 |
|------|------|
| 拖拉机检测算法复杂 | 先写穷举检测，再优化；大量单元测试覆盖边界 |
| 叫主规则变体多 | 设计可配置的规则引擎 |
| 前端牌桌 UI 交互复杂 | 先用简单 div 渲染，逐步优化动画 |
| 多人联机时序问题 | 服务端权威验证（不信任客户端） |
| 甩牌合法性判断难 | 实现完整的回溯搜索算法，确认所有对手都管不上 |

---

## 九、MVP（最小可行产品）定义

如果时间有限，建议按以下 MVP 范围先交付可玩版本：

1. ✅ 两副牌发牌 + 基本卡牌类型
2. ✅ 叫主（固定级牌 2，简单轮流叫主）
3. ✅ 基本出牌（单张、对子、拖拉机，不支持甩牌）
4. ✅ 墩比较 + 完整计分（含抠底）
5. ✅ 升级循环（2→A）
6. ✅ 一个简单的命令行界面（CLI）+ 3 个 AI 对手
7. ✅ 基本 Web 界面

**MVP 目标**：能在浏览器或终端完成一局完整的升级游戏（从发牌到 A 升级完成）。

---

> 📌 **下一步建议**：如果你认可这个计划，我们可以从 **Phase 1** 开始，先在 `packages/core/` 下初始化 TypeScript 项目，然后逐步实现各个模块。

